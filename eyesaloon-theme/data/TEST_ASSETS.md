# Test Product Assets

The images in `data/product-images/test-*.jpg` are AI-generated test assets for development only.

They are not real Eyesaloon inventory photos and should be replaced before launch.

The matching products in `data/products.csv` use `test-*` handles, SKUs, and titles so they can be removed or replaced cleanly.
